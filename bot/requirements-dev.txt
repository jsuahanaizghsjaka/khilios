-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.25.0

# Безопасность. Обе проверки стоят в CI, потому что оба класса проблем
# приходят молча: уязвимость — с обновлением транзитивной зависимости,
# инъекция — с невинной правкой запроса.
pip-audit==2.9.0
bandit==1.9.4
