# Версии пинуются точно. Бот переживает падение ноды, но не переживёт
# молчаливое обновление aiogram, которое сломает хендлеры в три часа ночи.
aiogram==3.30.0
aiosqlite==0.20.0
httpx==0.27.2

# aiohttp — транзитивная зависимость aiogram, и пин здесь не лишний.
# aiogram допускает весь диапазон <3.15, а нижняя его часть содержит
# известные уязвимости: на aiogram 3.15 подтягивался aiohttp 3.10.11
# с длинным списком CVE. Проверять так:
#   pip-audit -r requirements.txt
aiohttp==3.14.3
