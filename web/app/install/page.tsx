import type { Metadata } from "next";
import { SERVICE } from "@/lib/config";

export const metadata: Metadata = { title: "Установка" };

// Каркас. По плану заполняется 1 сентября — гифками, а не текстом.
// Это главный экономитель поддержки: инструкция с картинками режет
// обращения примерно вдвое, поэтому резать саму страницу нельзя,
// даже если график поехал.

const PLATFORMS = [
  {
    id: "ios",
    title: "iPhone и iPad",
    steps: [
      "Установить приложение из App Store",
      "Открыть ссылку из бота — приложение подхватит настройки само",
      "Разрешить добавление конфигурации VPN",
      "Включить переключатель",
    ],
  },
  {
    id: "android",
    title: "Android",
    steps: [
      "Установить приложение",
      "Открыть ссылку из бота",
      "Разрешить подключение",
      "Отключить экономию батареи для приложения, иначе система будет рвать соединение в фоне",
    ],
  },
  {
    id: "windows",
    title: "Windows",
    steps: [
      "Скачать приложение",
      "Вставить ссылку на подписку из бота",
      "Подключиться",
    ],
  },
  {
    id: "macos",
    title: "macOS",
    steps: [
      "Установить приложение",
      "Открыть ссылку из бота",
      "Разрешить конфигурацию в системных настройках",
    ],
  },
];

export default function Install() {
  return (
    <>
      <h1>Установка</h1>

      <p className="lead">
        Ссылку на подписку выдаёт бот сразу после оплаты или запуска пробного
        периода. Настраивать вручную ничего не нужно.
      </p>

      {PLATFORMS.map((p) => (
        <section key={p.id}>
          <h2>{p.title}</h2>
          <ol className="reasons">
            {p.steps.map((step, i) => (
              <li key={i}>{step}</li>
            ))}
          </ol>
        </section>
      ))}

      <h2>Не получилось</h2>

      <p>
        Напишите в поддержку {SERVICE.supportHours}. Скажите, какое устройство
        и на каком шаге остановились — этого хватит, чтобы разобраться.
      </p>
    </>
  );
}
