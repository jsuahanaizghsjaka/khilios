// Единственное место, где живут цены, ссылки и расписание поддержки.
// Меняется здесь — меняется на всех страницах.

export const SERVICE = {
  name: "khilios",

  // Бот появится после недели 3. Канал уже есть, и он захардкожен намеренно:
  // на него ссылается половина обещаний сайта («о сбоях пишем сами»), и
  // ссылка не должна пропадать из-за незаданной переменной на Vercel.
  bot: process.env.NEXT_PUBLIC_BOT_URL || "https://t.me/khilios_vpn_bot",
  channel: process.env.NEXT_PUBLIC_CHANNEL_URL ?? "https://t.me/khilios_vpn",

  // Написано на сайте намеренно. Люди нормально относятся к честному
  // расписанию и плохо — к молчанию. Круглосуточная поддержка одним
  // человеком заканчивается выгоранием, а не довольными пользователями.
  supportHours: "с 9:00 до 11:00 и с 19:00 до 22:00 МСК",
  supportPromise: "Отвечаем в течение двух часов внутри окна.",
};

export type Plan = {
  id: string;
  name: string;
  price: number;
  period: string;
  devices: number;
  note?: string;
  featured?: boolean;
};

// period — готовая строка для показа, а не единица измерения:
// подставлять предлог в шаблоне пришлось бы с исключением на пробный тариф.
export const PLANS: Plan[] = [
  {
    id: "trial",
    name: "Пробный",
    price: 0,
    period: "7 дней",
    devices: 1,
    note: "Без карты и без автосписания. Карту вообще не спрашиваем.",
  },
  // Подписи есть у всех четырёх намеренно: в сетке 2×2 карточка без подписи
  // получает пустой низ, и «Стандарт» — тот, куда ведём, — выглядел самым
  // недоделанным из четырёх.
  {
    id: "basic",
    name: "Базовый",
    price: 199,
    period: "в месяц",
    devices: 2,
    note: "Если устройств немного: телефон и ноутбук.",
  },
  {
    id: "standard",
    name: "Стандарт",
    price: 299,
    period: "в месяц",
    devices: 5,
    featured: true,
    note: "Хватает на всю семью. Лимит честный: пять — это пять.",
  },
  {
    id: "year",
    name: "Год",
    price: 1990,
    period: "в год",
    devices: 5,
    note: "166 ₽ в месяц.",
  },
];

export const REFUND_DAYS_RULE =
  "Возвращаем пропорционально неиспользованным дням, по первому запросу, без выяснения причин.";
